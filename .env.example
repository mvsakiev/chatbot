OPENAI_API_KEY=sk-...            # для фолбэка (можно не указывать, если python-сервис доступен)
OPENAI_MODEL=gpt-4o-mini
PORT=8787
PY_SERVICE_URL=http://localhost:8000
# Разрешённые Origin для CORS (через запятую). Например, адрес фронта на Render
CORS_ORIGIN=http://localhost:5500,http://localhost:8787
